````markdown
# Implementation Log - <slug>

> This document records implementation iterations for Spec-Driven Planning. Update it every time work is performed so stakeholders can trace which tasks were executed, how, and with what results.

## Meta
- Requirement slug: `<slug>`
- Last updated: `<YYYY-MM-DD HH:MM>`
- Reference docs: `requirement.md`, `design.md`, `tasks.yml`, `product.md`, `tech.md`, `structure.md`

## Iteration Summary
| Timestamp | Tasks | Implementer | Test Result | Key Outcomes |
|-----------|-------|-------------|-------------|---------------|
| `<YYYY-MM-DD HH:MM>` | `T-001, T-002` | `Claude Code` | `✅ / ⚠️ / ❌` | `Headline result` |

## Iteration Details

### Iteration `<YYYY-MM-DD HH:MM>`
- Implementer: Claude Code
- Tasks executed: `T-001, T-002`
- Effort: `<total estimated hours>`
- Summary: `<2-3 sentences on delivered value>`

#### Task Logs

##### T-001 <Task Title>
- **Goal**: <1-2 sentence summary of objective>
- **Context**: <Design/steering references consulted>
- **Work Performed**:
  - <Primary code changes or artifacts>
  - <Supporting updates (configs, docs, scripts)>
- **Files Changed**:
  - `<path/to/file>`: <role or change summary>
  - `<another/file>`: <role or change summary>
- **Definition of Done**:
  - [x] <Describe how each DoD item was validated>
  - [ ] <If unmet, explain reason and follow-up>
- **Testing**:
  - Command: `<command>`
  - Result: `✅ Pass / ⚠️ Warning / ❌ Fail`
  - Log summary: <Root cause for failures or key metrics for success>
- **Risks / TODO**:
  - <Follow-up observations, mitigation steps, remaining unknowns>

(Repeat the same structure for each selected task)

#### Artifact Inventory
| File | Type | Purpose / Content | Notes |
|------|------|-------------------|-------|
| `<path/to/file>` | `new/updated/removed` | `<short description>` | `<caveats>` |

#### Test Execution Details
- Commands executed:
  - `<command>`
- Environment: `<local / CI / etc>`
- Additional logs: `<include excerpts as needed>`

#### Outstanding Items & Next Actions
- [ ] <Follow-up required>
- [ ] <Review or PR request>
- [ ] <Release readiness tasks>

---

(Continue appending future iterations using the same structure)
````